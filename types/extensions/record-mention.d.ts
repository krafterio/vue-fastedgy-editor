/**
 * A mention as it is written: tiptap's own, its suggestions included, drawn and
 * stored as the node a document reads (cf `mention.js`).
 */
export const RecordMention: any;
//# sourceMappingURL=record-mention.d.ts.map